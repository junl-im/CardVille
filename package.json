{
  "name": "cardville",
  "version": "1.0.21",
  "private": true,
  "type": "module",
  "description": "CardVille 1.0.21 fixes duplicated start overlay/video play marker/back navigation UX, compacts login controls, polishes cards, and strengthens association/security checks.",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "check:deploy": "node tools/check-deploy.mjs",
    "verify": "npm run build && npm run check:deploy && npm run check:brand && npm run check:ui && npm run check:layout && npm run check:association && npm run check:security",
    "check:brand": "node tools/check-brand.mjs",
    "check:ui": "node tools/check-ui.mjs",
    "check:layout": "node tools/check-layout.mjs",
    "check:security": "node tools/check-security.mjs",
    "check:association": "node tools/check-association.mjs"
  },
  "dependencies": {
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "typescript": "5.8.3",
    "vite": "6.3.5"
  }
}
