{
  "name": "cardville",
  "version": "0.9.0",
  "private": true,
  "description": "CardVille - premium Dream Library card puzzle and collection game with intro video, local guest fallback and generated asset seed pack.",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc && vite build",
    "preview": "vite preview --host 0.0.0.0",
    "typecheck": "tsc --noEmit",
    "check:imports": "node tools/check-relative-imports.mjs",
    "check:json": "node tools/check-json-data.mjs",
    "verify": "npm run check:imports && npm run check:json && npm run check:assets && npm run build && npm run check:budget",
    "check:assets": "node tools/check-assets-policy.mjs",
    "check:budget": "node tools/check-bundle-budget.mjs"
  },
  "dependencies": {
    "firebase": "12.15.0",
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "2.1.0",
    "typescript": "5.8.3",
    "vite": "6.3.5"
  }
}
